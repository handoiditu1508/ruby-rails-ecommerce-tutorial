<h1>Edit User</h1>

<%= render "/users/form", model: [:admin, @user], show_audit_fields: true %>

<%= link_to "Delete", [:admin, @user], data: {
  turbo_method: :delete,
  turbo_confirm: "Are you sure?"
} %>
