<h1>Edit User</h1>

<%= render "/users/form", model: [:admin, @user], user: @user, hide_password_field: true %>

<%= link_to "Delete", [:admin, @user], data: {
  turbo_method: :delete,
  turbo_confirm: "Are you sure?"
} %>
