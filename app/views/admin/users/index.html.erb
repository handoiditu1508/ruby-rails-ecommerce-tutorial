<h1>Manage Users</h1>

<ul>
  <% @users.each do |user| %>
    <li>
      <%= link_to "%s - %s" % [user.username, user.role.name], edit_admin_user_path(user) %>
    </li>
  <% end %>
</ul>

<%= link_to "New", new_admin_user_path %>
